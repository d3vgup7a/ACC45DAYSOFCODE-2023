#include <stdio.h>

int main(void) {
  int t, w, x, y, z, bal;

  scanf("%d", &t);

  while (t--) {
    scanf("%d %d %d %d", &w, &x, &y, &z);

    bal = w + (x*z) - (y*z);

    printf("\n%d", bal);
  }
}
