#include <stdio.h>

int main(void) {
  int t, a, b, c, x;

  scanf("%d", &t);
  while (t--) {
    scanf("%d %d %d %d", &a, &b, &c, &x);

    if ((a + b >= x) || (b + c >= x) || (c + a >= x))
      printf("\nYES");
    else
      printf("\nNO");
  }
}
