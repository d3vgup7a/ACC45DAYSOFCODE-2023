#include <stdio.h>

int main() {
  int t, n;

  scanf("%d", &t);

  while (t--) {
    scanf("%d", &n);

    if (((n % 2) == 0) || (n == 1)) {
      printf("\nYES");
    }
    else {
      printf("\nNO");
      
    }
  }
  return 0;
}
