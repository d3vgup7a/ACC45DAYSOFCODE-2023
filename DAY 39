#include <stdio.h>

int main() {
  int t, a1, a2, b1, b2;

  scanf("%d", &t);

  while (t--) {
    scanf("%d %d %d %d", &a1, &a2, &b1, &b2);

    if ((a2 + b2 - a1 - b1) > 0) {
      printf("\nYES");
    }
    else {
      printf("\nNO");
    }
  }
return 0;
}
