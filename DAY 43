#include <stdio.h>

int main() {
  int t, m, n;

  scanf("%d", &t);

  while (t--) {
    scanf("%d %d", &n, &m);
    printf("%d", (n - (n/10)));

    if (m > (n - (n/10))) {
      printf("\nONLINE");
    }
    else {
      if (m < (n - (n/10))) {
        printf("\nDINING");
      }
      else {
        printf("\nEITHER");
      }
    }
  }
  return 0;
}
