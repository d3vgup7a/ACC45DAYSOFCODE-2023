#include <stdio.h>

int main() {
  int t, n , m;

  scanf("%d", &t);

  while (t--) {
    scanf("%d %d", &n, &m);

    if (n < m) {
      printf("\n%d", n);
    }
    else {
      printf("\n%d", (n - m) + n);
    }
  }
}
